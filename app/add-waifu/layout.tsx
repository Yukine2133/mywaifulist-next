export const metadata = {
  title: "Add Waifu",
  description: "Add your waifu to others to see.",
};

export default function AddWaifuLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
